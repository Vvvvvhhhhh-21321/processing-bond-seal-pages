# 05 — 在回拼流程中补齐落款日期

**What to build:** 让用户在第二阶段提供统一落款日期，系统在不覆盖已有内容的前提下补齐回章页缺失的年、月、日，并把日期结果纳入批次状态。

**Blocked by:** 03 — 完成文字层回章页的自动回拼。

**Status:** resolved

- [x] 统一落款日期为可选输入；未提供时不进行日期写入。
- [x] 页面已有的年、月或日保持原值，只补齐缺失的组成部分。
- [x] 新增日期与页面已有内容发生碰撞时按既定最小字号范围尝试安全落位。
- [x] 无法安全落位的日期组成部分不写入，并记录具体失败原因。
- [x] 日期补齐失败不阻止已匹配回章页完成回拼，也不阻止其他文件处理。
- [x] 日期写入不改变印章、原有文字和页面尺寸。
- [x] 测试覆盖全部空白、部分已有、全部已有、碰撞缩小和无法落位场景。

## Answer

已在提交 `cc215ff` 与 `801bb3b` 中完成第二阶段日期补齐。`complete_processing_batch(...)` 接受可选统一日期，按页面最下方同一行的“年/月/日”落款锚点逐项判断，只补缺失值；存在碰撞时在不低于 0.85 的范围内缩小，无法安全落位时保留原回章页并记录结构化日期状态与中文原因，不影响页面回拼及批次其他文件。

验证结果：任务5定向测试 11 项通过；完整测试共 76 项，75 项通过、1 项真实 Word 烟雾测试因环境条件跳过。代表性 PDF 已使用 Poppler 渲染并目视确认日期基线、印章、原文字及页面尺寸正常。Code Review 的 Standards 与 Spec 两条审查均无阻塞。
