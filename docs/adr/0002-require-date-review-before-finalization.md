# 回拼前必须确认日期确认稿

ADR-0001 的回章阶段细分为两个有持久化接缝的步骤：系统先完成 OCR、匹配和落日期，生成保持原页数与顺序的日期确认稿；用户看到确认稿并明确确认后，系统才复用首次匹配关系回拼。用户可修改确认稿的页面内容，但不得增删或调序；最终回拼不重新 OCR、匹配或落日期。这一确认关口牺牲了一次交互，以避免用户尚未检查日期时生成正式底稿，也避免确认后重新计算导致结果漂移。
