interface:
  display_name: "债券底稿盖章页处理"
  short_description: "可选哈希去重，生成待盖章页、日期确认稿并回拼"
  default_prompt: "使用 $processing-bond-seal-pages 处理债券底稿盖章页：判断是生成处理批次、生成日期确认稿并暂停，还是在用户确认后回拼；prepare 前确认用户选择保留全部 Word 或按文件哈希排除重复文件，再执行对应前置检查。"
